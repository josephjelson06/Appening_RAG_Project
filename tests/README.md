# Tests

Place automated unit and integration tests for the `rag` package here.

The current repository validation also includes a local smoke check for Python
compilation, API imports, LangGraph construction, and deterministic extraction
and chunk creation.
